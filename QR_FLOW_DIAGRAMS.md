# QR Ordering System - Visual Flow Diagram

## System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                      App.tsx                                 │
│  ┌────────────────────────────────────────────────────┐     │
│  │              OrderProvider                          │     │
│  │  • tableNumber: string | null                       │     │
│  │  • orderType: 'dine-in' | 'takeaway' | null        │     │
│  │  • isCheckoutLocked: boolean                        │     │
│  │  • resetOrder()                                     │     │
│  │  ┌──────────────────────────────────────────────┐  │     │
│  │  │          CartProvider                         │  │     │
│  │  │  • items: CartItem[]                          │  │     │
│  │  │  • addItem()                                  │  │     │
│  │  │  • removeItem()                               │  │     │
│  │  │  • clearCart()                                │  │     │
│  │  └──────────────────────────────────────────────┘  │     │
│  └────────────────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────────────┘
```

## Component Hierarchy

```
Pages
├── Index (/)
│   └── FloatingQRButton
│       └── QRScannerModal
│
├── Menu (/menu)
│   ├── Table Indicator Banner
│   │   └── Clear Table Button (if !isCheckoutLocked)
│   ├── DishCard (multiple)
│   │   └── OrderTypeModal
│   │       ├── Tab: Dine-in → QRScannerModal
│   │       └── Tab: Takeaway
│   └── FloatingQRButton
│       └── QRScannerModal
│
└── Checkout (/checkout)
    ├── Order Info (table/orderType)
    ├── Cart Items
    └── Confirm Checkout Button
        └── Sets isCheckoutLocked = true
```

## User Flow Diagrams

### Flow 1: Dine-in via Floating QR Button

```
┌─────────────┐
│   User      │
│  Opens App  │
└──────┬──────┘
       │
       v
┌─────────────────┐
│  Index Page     │
│  (Home)         │
│  ┌───────────┐  │
│  │ Floating  │  │
│  │ QR Button │◄─┼─── User clicks
│  └───────────┘  │
└────────┬────────┘
         │
         v
┌────────────────────┐
│  QR Scanner Modal  │
│  📷 Camera Active  │◄─── User scans QR code
└────────┬───────────┘
         │
         v
┌────────────────────┐
│  Parse QR Code     │
│  "TABLE-5"         │
│  ↓                 │
│  tableNumber = "5" │
│  orderType = "dine-in"
└────────┬───────────┘
         │
         v
┌────────────────────┐
│  Navigate to       │
│  /menu             │
└────────┬───────────┘
         │
         v
┌────────────────────┐
│  Menu Page         │
│  ┌──────────────┐  │
│  │ Table: 5     │  │◄─── Table indicator visible
│  │ • Dine-in    │  │
│  │ [Clear]      │  │
│  └──────────────┘  │
│                    │
│  [Dish Cards]      │◄─── User can add items
└────────────────────┘
```

### Flow 2: Takeaway via Add to Cart

```
┌─────────────┐
│   User      │
│  Opens App  │
└──────┬──────┘
       │
       v
┌─────────────────┐
│  Menu Page      │
│  [Dish Card]    │
│  ┌───────────┐  │
│  │ Add to    │  │
│  │ Order     │◄─┼─── User clicks
│  └───────────┘  │
└────────┬────────┘
         │
         v
┌────────────────────┐
│  Check State       │
│  tableNumber? NO   │
│  orderType? NO     │
└────────┬───────────┘
         │
         v
┌────────────────────────────┐
│  OrderTypeModal            │
│  ┌──────────┬──────────┐   │
│  │ Dine-in  │ Takeaway │   │
│  └──────────┴──────────┘   │
│                            │
│  Tab 1: Dine-in            │
│  ┌──────────────────────┐  │
│  │ Scan QR to Select    │  │
│  │ Table                │  │
│  └──────────────────────┘  │
│                            │
│  Tab 2: Takeaway           │
│  ┌──────────────────────┐  │
│  │ Proceed as Takeaway  │◄─┼─── User clicks
│  └──────────────────────┘  │
└────────┬───────────────────┘
         │
         v
┌────────────────────┐
│  Set State         │
│  orderType =       │
│  'takeaway'        │
└────────┬───────────┘
         │
         v
┌────────────────────┐
│  Add Item to Cart  │
│  Close Modal       │
└────────┬───────────┘
         │
         v
┌────────────────────┐
│  Menu Page         │
│  ┌──────────────┐  │
│  │ Order Type:  │  │◄─── Indicator visible
│  │ Takeaway     │  │
│  │ [Clear]      │  │
│  └──────────────┘  │
└────────────────────┘
```

### Flow 3: Checkout Lock

```
┌─────────────────┐
│  Menu Page      │
│  Table: 5       │
│  Cart: 3 items  │
└────────┬────────┘
         │
         v
┌─────────────────┐
│  Navigate to    │
│  /checkout      │
└────────┬────────┘
         │
         v
┌────────────────────────────┐
│  Checkout Page             │
│  ┌──────────────────────┐  │
│  │ Order Type:          │  │
│  │ Dine-in - Table 5    │  │
│  └──────────────────────┘  │
│                            │
│  [Cart Items]              │
│  Total: $45.50             │
│                            │
│  ┌──────────────────────┐  │
│  │ Confirm Checkout     │◄─┼─── User clicks
│  └──────────────────────┘  │
└────────┬───────────────────┘
         │
         v
┌────────────────────┐
│  Set State         │
│  isCheckoutLocked  │
│  = true            │
│  clearCart()       │
└────────┬───────────┘
         │
         v
┌────────────────────┐
│  Navigate to       │
│  /orders           │
└────────┬───────────┘
         │
         v
┌────────────────────┐
│  Back to Menu      │
│  ┌──────────────┐  │
│  │ Table: 5     │  │
│  │ • Dine-in    │  │
│  │ [Clear] 🚫   │◄─┼─── Button DISABLED
│  └──────────────┘  │     (checkout locked)
└────────────────────┘
```

## State Transitions

```
Initial State
┌─────────────────────┐
│ tableNumber: null   │
│ orderType: null     │
│ isCheckoutLocked:   │
│   false             │
└──────────┬──────────┘
           │
           ├─────────────────────┐
           │                     │
           v                     v
    Scan QR Code         Select Takeaway
┌─────────────────────┐  ┌─────────────────────┐
│ tableNumber: "5"    │  │ tableNumber: null   │
│ orderType:          │  │ orderType:          │
│   'dine-in'         │  │   'takeaway'        │
│ isCheckoutLocked:   │  │ isCheckoutLocked:   │
│   false             │  │   false             │
└──────────┬──────────┘  └──────────┬──────────┘
           │                        │
           └────────┬───────────────┘
                    │
                    v
            Add Items to Cart
                    │
                    v
            Proceed to Checkout
                    │
                    v
            Confirm Checkout
┌─────────────────────┐
│ tableNumber: "5"    │  or  tableNumber: null
│ orderType:          │      orderType: 'takeaway'
│   'dine-in'         │
│ isCheckoutLocked:   │
│   TRUE ✓            │◄──── LOCKED STATE
└─────────────────────┘
           │
           │ Clear Table Button
           │ DISABLED
           v
    Cannot Change Table
```

## Component Interaction Flow

```
FloatingQRButton
    │ onClick
    v
QRScannerModal
    │ onScan
    v
OrderContext.setTableNumber("5")
OrderContext.setOrderType('dine-in')
    │
    v
Navigate to /menu
    │
    v
Menu Page
    │ renders
    v
Table Indicator (shows table info)
    │
    └──> Clear Button (visible if !isCheckoutLocked)
    │
    v
DishCard
    │ onClick "Add to Order"
    v
Check: tableNumber || orderType === 'takeaway'
    │
    ├─ YES ──> Add to Cart
    │
    └─ NO ──> Show OrderTypeModal
              │
              ├─ Dine-in ──> QRScannerModal
              │
              └─ Takeaway ──> Set orderType, Add to Cart
```

## localStorage Structure

```javascript
{
  "saltbee-table-number": "5",           // or null
  "saltbee-order-type": "dine-in",       // or "takeaway" or null
  "saltbee-checkout-locked": "true",     // or "false"
  "saltbee-cart": "[{...items}]"         // from CartContext
}
```

## QR Code Format Examples

```
Valid QR Codes:
┌─────────────┐
│  TABLE-5    │  → tableNumber = "5"
└─────────────┘

┌─────────────┐
│  TABLE5     │  → tableNumber = "5"
└─────────────┘

┌─────────────┐
│     5       │  → tableNumber = "5"
└─────────────┘

┌─────────────┐
│  table-12   │  → tableNumber = "12"
└─────────────┘

Invalid QR Codes:
┌─────────────┐
│  HELLO      │  → Error: Invalid QR code
└─────────────┘

┌─────────────┐
│  ABC123     │  → Error: Invalid QR code
└─────────────┘
```

## Error Handling Flow

```
QR Scanner
    │
    ├─ Camera Permission Denied
    │       │
    │       v
    │   Show Error: "Camera access denied"
    │
    ├─ Invalid QR Code
    │       │
    │       v
    │   Show Error: "Invalid QR code"
    │
    └─ Valid QR Code
            │
            v
        Extract Table Number
            │
            v
        Set State & Navigate
```
